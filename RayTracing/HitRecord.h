#pragma once
/*
class HitRecord
{

public :
	HitRecord(float t, glm::vec3 &P, glm::vec3 &N, std::shared_ptr<Material> matPtr) : m_t(t), m_P(P), m_N(N), m_matPtr(matPtr) {}


private :
	// t value for the equation of the Ray
	float m_t;
	// Point that have been hit
	glm::vec3 m_P;
	// Normal of the hit point
	glm::vec3 m_N;
	// Material found
	std::shared_ptr<Material> m_matPtr;

	// TODO : bool to check if the intersection is valid ?

};*/